main: re.imc.geysermodelengine.GeyserModelEngine
name: GeyserModelEngine
version: '${version}'
api-version: '1.21'

# if it works it works, if not it's not
folia-supported: true

authors:
  - zimzaza4
  - willem.dev
  - xSquishyLiam

load: STARTUP

dependencies:
  server:
    GeyserUtils:
      required: true
    packetevents:
      required: true
    floodgate:
      required: false
    ModelEngine:
      required: false
    BetterModel:
      required: false